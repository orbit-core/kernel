namespace: OrbitCoreTest\Kernel

paths:
  tests: .
  data: ../../_data
  support: _support
  log: ../../_output

coverage:
  enabled: true
  remote: false
  whitelist:
    include:
      - '../../../src/*'

suites:
  Domain:
    path: Domain
    class_name: KernelDomainTester
    modules:
      enabled:
        - Asserts
